# SOKUGAN 3.2 — プロジェクト規約

日本語速読トレーニングアプリ。ユーザーはWon（韓国語ネイティブ・22歳から日本語・WOTA水処理スタートアップ経営責任者）。
**目的**: 後発の日本語学習者が、理解を保ったまま教育あるネイティブ以上の速さで読めるようになること。

## 構成
| パス | 役割 |
|---|---|
| `index.html` | アプリ本体（単一HTML・PWA）。localStorage `sokugan_v3` が正本。任意でクラウド同期 |
| `sokugan-config.js` | 外部サービス設定。**公開して安全な anon key のみ**（空でも動く） |
| `supabase/schema.sql` | 同期テーブル定義とRLS。`supabase/セットアップ手順.md` に導入手順 |
| `daily-content.json` | その日の教材10本（毎日入れ替え・5系統×2本） |
| `sources.json` | **素材をどこから拾うかの正本**。論考・株主レター・研究・実務者コミュニティを先に巡回し、WebSearchは補助（A18〜A18f） |
| `clarity-audit.json` | 設問明快さ監査の記録（Dゲート） |
| `qa-report.json` | QAハーネス結果（アプリが読んでバッジ表示） |
| `qa/harness.js` | **329項目のQA検査**（依存ゼロのnode） |
| `qa/repair-chunks.js` | チャンク/genre自動修復 |
| `manifest.json` `sw.js` `icons/` | PWA（スマホのホーム画面アプリ化） |

## 絶対ルール
1. **QAハーネス全PASSでなければ公開（push）しない**。`node qa/harness.js` の終了コード0が唯一の合格条件。偽装PASS禁止。
2. **`chunks` はLLMが書かない**。`index.html` 内の決定論チャンカー（`__CHUNKER_START__`〜`__CHUNKER_END__`）が本文から毎回生成する。過去にLLM生成で語中分割の事故が頻発した。
3. **数字は算用数字**（2024年・6,300ドル・300億ドル）。漢数字の数量表記は禁止（A4cが検査）。アプリ側でも表示時に正規化する。
4. **ジャンルは5系統固定**: スタートアップ・新規事業／社会・価値観／市場・経済・地政学／経営・リーダーシップ／未来の兆し。科学・論文ジャンルは廃止（A3b）。
5. **AI中心の記事は最大2本、非AIが8本以上**（A14）。全系統がAIの話に寄るのを禁止。
6. **設問は一読で分かる平易な日本語**。翻訳調・「の」の多重連結・抽象語の羅列を禁止（A6bリンター）。
7. `index.html` を変更したら必ずハーネスを再実行する。
8. **既読本文は再表示しない**。既読化は「読み終えた時点」で行う（開始時に確定させると中断分まで在庫が消える）。未読が尽きたら既読へ戻さず「本日分を完走」と表示する。
9. **色の意味を混ぜない**。朱は「今ここ・未達」、緑は「正解・達成」だけに使う。正解を朱で示さない（B18eが焦点色と正答色の分離を検査）。
10. **同期は上書きで解決しない**。集合型（history/seen/wins/flags/anchor）はunion＋重複排除、記録型（PB/最大連続）はmax、設定型はupdatedAt/fieldTsの新しい方。端末Aの記録が端末Bの同期で消える実装は禁止（F2〜F12が検査）。
11. **`sokugan-config.js` に置けるのは anon key だけ**。service_role key や秘密情報をクライアントへ置かない（F14が検査）。

## 教材の質の基準
- **タイトルは要約ではない**。具体的な事実を1つ置き、その事実の意外な含意を同じ一文に収める。18〜36字・読点1つまで・桁区切りの生数字禁止（1万以上は万/億）・同じ構文は10本中2本まで（A16/A16b/A16cが検査）。
- 本文450〜650字、「事実→構造→含意」、常体。**選定基準は「読んだその日に誰かへ『実は…』と話したくなるか」**。直近30日分のarchiveとタイトル・論点・出典URLを照合し、既報の言い換えを作らない。
- 各パッセージに `keyTerms`（語彙道場用・**12歳にわかる説明**＋誤答3つ）と `takeaway`（持ち帰りフック10〜70字）が必須（E1/E2）。
- **素材は sources.json から拾い、WebSearchは補助**。検索は鮮度で並ぶので、そこから始めると必ずプレスリリースが上に来る。直近30日の教材は出典の9割が発表者本人のページ、論考1%、独立系ニュースレター0本だった。選定ロジックを直しても候補が発表ものしか無ければ変わらない。2026-09-11以降の教材は official 3本以下・洞察系5本以上（A18b）、出典URL必須（A18）
- **誰が書いたかを本文の前に出す**（`author: {name, role}`、A19/B12b）。洞察は書き手の立場で重みが変わる。人名が分からないときは媒体名・組織名にし、推測で人名を書かない
- **出典ドメイン別の選択率**（`sourceDomains`）を見て sources.json を増減する。「どこから拾うと読まれるか」を学習の対象にする
- **選定の主信号は「選ばなかった」記録**（`pickSignals`）。読後評価は109本読んで6件しか付かず、ジャンル別の最小サンプルにも届かないため選定を一度も変えていなかった。一覧の10本から2本選ぶ行為は毎日必ず起きるので、選択率をプロファイルの第一指標にする（F16f）
- **選択率が低いジャンルは本数を減らさず、角度を変える**。選ばれないのは興味が無いからではなく、出している切り口が合っていない可能性が高い
- **驚きを文型で作らない**。「AではなくB」の対比フックは10本中3本まで（A17）。直近30日で48%がこの型に落ち、内容に関係なく飽きる状態になっていた
- **発表もの（「〜が〜を発表した」で始まる記事）を増やさない**。A17bは現状維持の床（7本）で、生成側の目標は3本以下。次の生成で下がったら床も下げる
- 読後の**自由記述**（1件200字まで）は選択式評価と並んで保存され、`tools/export-content-feedback-profile.js` の `notes[]` として翌日の選定へ渡る。要約せず本人の言葉のまま扱う（B18j/F16e）。
- **設問は2問**。1問目で全体の論旨（`Take-away:`）、2問目でその根拠（`Strong evidence:`）。4択・22〜32字・単独最長=正解禁止・rationale 50〜90字。内容を理解しながら読んでいれば解ける水準にし、重箱の隅は作らない（A5/A5b/A5c）。

## コマンド（このリポジトリで使えるもの）
| コマンド | 用途 | 自動実行 |
|---|---|---|
| `/daily` | 翌日分の教材を生成 → 明快さ自己監査 → ハーネス → 公開 | 毎日18:00 JST（GitHub Actions） |
| `/qa` | 生成物を監督（**自分で解いて検証**・書き直し）→ ハーネス → 公開 | 毎日19:00 JST（GitHub Actions） |
| `/feature <内容>` | 機能追加・改善（リサーチ→実装→回帰チェック追加→公開） | 手動 |
| `/fix <症状>` | バグ修正（再現→原因特定→修正→**再発防止の検査を追加**） | 手動 |
| `/report [path]` | 学習実績のバックアップJSONを分析してレポート化 | 手動 |
| `/publish` | ハーネス合格を確認して公開のみ | 手動 |

検査だけしたいとき: `node qa/harness.js`

## サブエージェント
- `research-analyst` — 学習科学の知見を**効果量と出典つき**で調査（改善案の根拠づけ）
- `content-scout` — 「誰かに話したくなる記事」だけを拾う素材審査

## QAゲート（機械的な強制）
`.claude/hooks/qa-gate.sh` が **`git push` の直前にQAハーネスを実行**し、不合格なら push を**拒否**する（`PreToolUse` フック）。
絶対ルール1は指示ではなく装置で守られている。壊れた教材が配信されることはない。

## リポジトリ構成（2つある・2026-08-31以降）

| remote | リポジトリ | 公開性 | 役割 |
|---|---|---|---|
| `origin` | `wonleekorea-lab/sokugan-workspace` | PRIVATE | **作業の正本**。履歴・規約・ツールはここ |
| `production` | `wonleekorea-lab/sokugan` | PUBLIC | **公開専用**。`main` の直下が GitHub Pages |

- 作業は必ず `origin` に対して行う。`production` は公開のために push するだけの出口で、**そこで作業しない**。
- 公開は `production/main` への push で GitHub Pages に反映され、スマホのPWAに届く（`https://wonleekorea-lab.github.io/sokugan/`）。
- **公開の順序は `origin` → QA合格 → `production`**。QA不合格のものを `production` に出さない。
- `production` は `origin` の早送りであること。`production` にだけ存在するコミットを作らない（作ると公開物と正本がずれる）。

## 日次更新の実行主体

- **現在の担当はローカルの launchd ジョブ** `local.wota.codex-sokugan-daily-refresh`（18:00 JST、`codex exec`）。
  両リモートへの push 権限を持つのがここだけなので、生成から公開まで一貫して行える。
  ログ: `~/Library/Logs/codex-launchd/`。設定: `~/.codex/launchd/`。
- `.github/workflows/` の2本は **cron を停止中**（`workflow_dispatch` のみ）。
  `GITHUB_TOKEN` はリポジトリを跨げず、このワークフローだけでは `production` に公開できないため。
  復活させるなら `production` への push 権限を持つ PAT を Secret に入れ、公開ステップを追加してから。
- ChatGPTアプリ内の Codex automation（`sokugan-daily-refresh`）は **PAUSED**。アプリを閉じると発火しないため launchd に移した。

## リモート同期

- `.claude/hooks/sync-check.sh` がセッション開始時に差分を確認し、作業ツリーがきれいなら自動でpullする。
  ずれの警告が出たら、作業を始める前に整理する（未コミット分をcommit → `git pull --rebase origin main`）。
- 作業の起点は常に **`origin/main` の最新**。古いローカルの上に実装を重ねない。

## 設計の根拠（変更時に壊さないこと）
- 理解度60%ゲート付き速度が唯一の北極星KPI（速度と理解のトレードオフを直視）
- **入口は「2本パッケージ」ひとつだけ**（B13e）。フル版が約6分になりショート版との差が消えた。毎回どちらかを選ばせること自体が続ける摩擦になる。short モードの実装は過去記録の表示と復活のために残す
- **1セッションは約6分**（キャッチ→本文①＋2問→反復読→本文②＋2問→結果）。3.5までのチャンクペーサーと語彙道場は3.6で流れから外した。コードは残してあるので、戻すときは `PHASES` に加えて `renderPacer` / `renderVocabIntro` を再び呼ぶ（B5/B6が「外れたまま」を固定している）
- **瞬間キャッチの選択肢は書き出しと字数を的に揃える**。書き出しが違う語が混ざると先頭1字だけで解け、語形認識の訓練にならない。的を先に決めず、同じ書き出しの候補が十分ある語だけを的にする（B10c/B10d）
- セッション所要（開始→採点の実時間）を毎回記録し、結果と記録タブに出す。同じ内容が短い時間で終わるほど習慣として続く（B16e/B17c/B18c2）
- **iOSではメールのリンクをタップさせない**。Safariで開くとホーム画面アプリと別の保存領域にログインが入り、アプリ側は未ログインのまま。リンクを貼り付けてアプリ内で認証を完了する経路を用意する（F13d/F13e）
- **同期パスワードは英数字8字以上**。覚えられない長さを強いると同期が使われず、記録がその端末で孤立する。忘れてもメールのリンクだけで戻れる道を常に残す（F13b/F13c）
- **記録が空の端末には必ず復元導線を出す**。iOSはホーム画面アプリを入れ直すと保存領域が変わり、記録が消えたように見える。正本はクラウド側に残っているので、ホームと記録タブの両方から戻れるようにする（F1c/F1d/F1e）
- 難度補正速度・一目量（字/固視）でプラトー期の実質成長を可視化
- 週次アンカー転移テストは訓練と分離（目標・ベストに影響させない）
- 語彙の深さ＝読解分散の約59%（Qian 2002）／自己説明 g=0.55／教えるつもり g=0.50／交互配置 g=0.42／具体的困難目標 d=0.42-0.80
